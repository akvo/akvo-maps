language: node_js

sudo: required

services:
  - docker

cache:
  directories:
    - "${HOME}/.cache"
    - "${HOME}/.m2"

before_install:
  - npm -version

script:
  - ./ci/build.sh

#after_success:
#  - ./ci/deploy.sh